# Generate with: (cd server && npm run hash-password -- 'your-password-here')
ADMIN_PASSWORD_HASH=

# Any long random string, e.g.: openssl rand -hex 32
SESSION_SECRET=

# Optional overrides
# PORT=3000
# TMUX_SOCKET_NAME=webterm
